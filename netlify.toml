[build]
  command = "npm run build"
  publish = "dist/tourism-app/browser"

[build.environment]
  NETLIFY_SKIP_PLUGINS = "@netlify/angular-runtime"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
